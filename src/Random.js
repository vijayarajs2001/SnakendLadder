import React from 'react'

export function randomFunction(){
    let diceOutput=Math.ceil(Math.random()*6)
    return diceOutput
}

 const Random = () => {
    
  return (
    <div>

    </div>
  )
}
export default Random